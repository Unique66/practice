迭代器模式：  提供一种方法顺序访问一个聚合对象中的各个元素，而又不暴露其内部的表示。
ListIterator  有 previous() 可以前向遍历

iterator 包下是自定义的iterator,有hasNext() ,next() 两个方法供不同的数据体实现来遍历数据体的数据

java 包下是Java自带的iterator ，原理与上面一样

单一责任原则：
设计原则：一个类应该只有一个引起变化的原因。
内聚（cohesion），它用来度量一个类或模块紧密地达到单一目的或责任，当一个模块或一个类被设计成只支持一组相关的功能时，
我们说它具有高内聚；反之，当被设计成支持一组不相关功能时，我们说它具有低内聚。